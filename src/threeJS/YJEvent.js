


class YJEvent {
	constructor() {
		// let eventList = [];
		// // 添加事件监听
		// this.addEvent = function (e, fn) {
		// 	eventList.push({ e: e, fn: fn });
		// }
		// // 执行事件
		// this.applyEvent = function (e, v, v2) {
		// 	for (let i = 0; i < eventList.length; i++) {
		// 		const element = eventList[i];
		// 		if (element.e == e) {
		// 			element.fn(v, v2);
		// 		}
		// 	}
		// }
		// this.eventList = [];
	}
 
	// 添加事件监听
	// addEvent(e, fn) {
	// 	// eventList.push({ e: e, fn: fn });
	// }
	// 执行事件
	// applyEvent(e, v, v2) {
	// 	for (let i = 0; i < eventList.length; i++) {
	// 		const element = eventList[i];
	// 		if (element.e == e) {
	// 			element.fn(v, v2);
	// 		}
	// 	}
	// }
}


export { YJEvent };